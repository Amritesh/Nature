import React from 'react';

const Grassland = () => {
  // Grass geometry removed for performance as requested.
  // The terrain texture/color (lush_grass biome) now represents the grassland area.
  return null;
};

export default Grassland;
